version: "3.8"

services:
  app:
    container_name: chanjoke-webapp
    build:
      context: .
      dockerfile: docker/Dockerfile.prod
    environment:
      - NODE_ENV=production
    ports:
      - 80:80